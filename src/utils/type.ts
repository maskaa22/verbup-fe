export type baseComponentType = {
  current: number,
  totalQuestions: number,
  img: string,
  question: string
}

export type writeGameType = {
  current: number,
  totalQuestions: number,
  text: string,
}